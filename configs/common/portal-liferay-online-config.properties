# Intentionally empty. Shadows the baked-in portal-liferay-online-config.properties
# from portal-impl.jar (which sets json.servlet.hosts.allowed=N/A and blocks JSONWS).
# Workspace-plugin's dockerDeploy task copies this file into the Docker image so the
# last-wins property loader replaces the DXP default before Liferay starts.
