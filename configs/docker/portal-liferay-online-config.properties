# Intentionally empty. Shadowing the baked-in portal-liferay-online-config.properties
# from portal-impl.jar, which sets json.servlet.hosts.allowed=N/A and blocks all JSONWS.
# LiferayContainer._copyPortalOnlineConfig() copies an empty file via /mnt/liferay/files/
# so the Docker entrypoint overwrites the baked-in default before Liferay starts.
